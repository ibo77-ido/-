# 2026-06-21 工作日志

## 源代码打包

- 将项目内所有源代码文件打包为 `Director_SourceCode_20260621.zip`，位于项目根目录
- 打包范围：Assets 下 .cs/.shader/.asmdef/.unity/.asset/.prefab/.json/.md/.txt 文件，以及 ProjectSettings/、Packages/、AGENTS/、Task/、Project/、Docs/ 目录全部文件，根目录 .csproj/.sln/.md
- 排除：美术资源（PNG/MP4/音频）、Library/、Temp/、Screenshots/ 等
- 结果：676 个文件，原始 7.40 MB，压缩后 **1.9 MB**，远小于 512MB 上限

## EXE 构建与修复

- 构建目标：StandaloneWindows64，仅包含 Phase9 SampleScene
- 构建路径：`C:/Users/lenovo/Desktop/景德镇窑火千年_Build/景德镇窑火千年.exe`，总大小 557MB
- 构建脚本：`Assets/Editor/CommandLineBuild.cs`
- **关键修复**：
  1. **URP 管线未分配**：GraphicsSettings.asset 的 `m_CustomRenderPipeline` 和 QualitySettings 所有等级的 `customRenderPipeline` 均为空（`{fileID: 0}`），导致构建产物使用内置管线而非 URP → 黑屏/崩溃。已修复为引用 `Assets/Settings/UniversalRP.asset`（GUID `681886c5eb7344803b6206f758bf0b1c`）
  2. **全屏模式**：`fullscreenMode` 从 `1`（Exclusive Fullscreen）改为 `3`（Fullscreen Window），避免独占全屏导致黑屏
  3. **窗口可调整**：`resizableWindow` 从 `0` 改为 `1`
  4. **后台运行**：`runInBackground` 从 `0` 改为 `1`
  5. **默认质量等级**：为 Standalone 平台设置默认质量等级为 `3`（High），而非之前无默认导致使用 Ultra
- **环境变量问题**：Bee/Tundra 构建系统 `env block error; too big?`，需用最小化环境变量启动 Unity（通过 Python subprocess 传递精简 env dict）

## EXE 崩溃修复（第二轮）

- **崩溃根因**：Player.log 显示路径乱码 `鏅痉闀囩獞鰏崈骞確Build`（UTF-8 字节被当作 GBK 解码），Unity 运行时无法找到自身 Managed DLL 和数据文件 → 启动即 Crash
- **修复**：将 `companyName` 和 `productName` 从中文"这个窑厂我包了"改为 ASCII "JingdezhenStudio" / "JingdezhenKiln"；构建路径从 `景德镇窑火千年_Build` 改为 `JingdezhenKiln_Build`
- **验证**：运行 EXE 10 秒未崩溃，Player.log 路径正确无乱码，无 Crash 文件夹生成
- **教训**：Unity Standalone Player 在中文 Windows 上不支持中文路径/产品名，会导致编码损坏
