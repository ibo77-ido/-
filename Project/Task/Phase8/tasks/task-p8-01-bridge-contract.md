# Task P8-01 Bridge Contract

## Goal

Freeze the bridge contract before implementation starts.

## Scope

- Confirm `Phase6 -> Bridge -> Phase3` as the only allowed dependency direction.
- Confirm the bridge is not a merged gameplay system.
- Confirm the bridge does not replace Phase3 gameplay rules.
- Confirm the bridge does not become a UI-only shell.

## Acceptance

- The contract is explicit and stable.
- No later task needs to re-open the dependency direction decision.
